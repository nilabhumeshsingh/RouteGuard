{
  "name": "routeguard-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "CampusSafe - Context-Aware Campus Navigation & Safety System",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "pnpm -r run lint",
    "seed": "tsx scripts/seed-demo.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "tsx": "^4.19.2",
    "vitest": "^2.1.8"
  },
  "packageManager": "pnpm@11.25.0"
}
